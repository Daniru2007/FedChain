# FedChain

A small Java workspace with a reusable `math.Matrix` utility and basic ML activation helpers.

## Run tests

```bash
mvn test
```

## Notes

- `math.Matrix` is immutable and returns new matrices from operations like `add`, `multiply`, and `transpose`.
- `ml.ActivationFunction` contains sigmoid, sigmoid derivative, and ReLU helpers.

